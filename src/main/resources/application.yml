server:
  port: 8012

spring:
  mvc:
    servlet:
      path: /cykb
  datasource:
    url: jdbc:mysql://122.51.193.191/cykb?useUnicode=true&characterEncoding=utf-8&serverTimezone=Asia/Shanghai&useSSL=false&autoReconnect=true&failOverReadOnly=false&allowPublicKeyRetrieval=true
    username: root
    password: 123456
  jpa:
    hibernate:
      ddl-auto: update
    database: mysql
    show-sql: false

logging:
  level:
    # 全局等级
    root: warn
    com: info
  file:
    name: logs/simple-blog.log